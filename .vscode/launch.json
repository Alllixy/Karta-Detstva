{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Запуск сервера",
            "program": "${workspaceFolder}/server.js"
        }
    ]
}